import { Typography } from "@mui/material";

const Header = () => {
  return (
    <header>
      <Typography component="h1" color="textPrimary" fontSize="20px">
        Cyrillic-Converter
      </Typography>
    </header>
  );
};

export default Header;
